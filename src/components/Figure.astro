---
export interface Props {
    icon: string;
}

const { icon, ...attributes } = Astro.props as Props;
/* @vite-ignore */
const { default: innerHTML } = await import(`../svg/${icon}.svg?raw`);
---

<Fragment {...attributes} set:html={innerHTML} />
